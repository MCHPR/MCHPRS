use libloading::Library;

struct ServerEventManager {
    
}

struct ServerPluginManager {
    libs: Vec<Library>,
}

impl ServerPluginManager {
    fn new_plot_manager(&self) -> PlotPluginManager {
        
    }
}

struct PlotPluginManager {

}